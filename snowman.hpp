#pragma once 
#include <string>
#include <stdexcept>
#include <stdio.h> 
#include <string.h> 
namespace ariel{
    std::string snowman(int dna);
}
bool isBase3(int num);
char reVal(int num) ; 
void strev(char *str);
int fromDeci(char res[], int base, int inputNum);


